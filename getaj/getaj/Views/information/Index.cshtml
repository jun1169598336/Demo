@model IEnumerable<getajdal.model.information>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.infoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.infoImg)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.typeid)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.infoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.infoImg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.typeid)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.infoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.infoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.infoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
